// Re-export of the shared @medialane/ui primitive (single source of truth,
// since 0.56.0). Local path kept so existing imports stay stable.
export { CoinLaunchPreview, type CoinPreviewData } from "@medialane/ui";
